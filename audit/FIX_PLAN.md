# Fix and rerun every result in `D:/SummerResearch`

Revised 2026-09-22. This replaces the earlier plan. Scope is the complete experiment, from original source data through new figures and tables. Existing CSVs, checkpoints, and `final_results.xlsx` are historical evidence; corrected runs get new output paths. No production code or dataset has been changed yet.

## 1. Freeze the exact experiment list and software version

Make one short run list from the paper/result tables and `src/synthesize_data/main.py`: dataset, split version, generator, label method, real/synthetic/mixed training, classifier, sample count, seed, and metric. Cover Adult, genuine Census KDD, Credit, Covertype, Intrusion, MNIST12, MNIST28, and News, plus every method combination actually reported. The current `census` files duplicate Adult and must not be counted as a second dataset. Record the 56k/14k and 60k/10k MNIST12 versions separately; select one split for the new comparison and derive MNIST12/28 from the same source-image split.

Pin one supported SDV/CTGAN/RDT/PyTorch environment and run its documented API checks before training. `requirements.txt` currently omits SDV entirely, and the active Python environment has no `sdv`, `ctgan`, or `rdt` installed. Current SDV examples use `Metadata` while this code imports `SingleTableMetadata`; use the API documented for the chosen version and prove it on a tiny table. Keep legacy SDGym's 2019 requirements out of this environment. Record package versions and effective synthesizer parameters in each run. Complete the run list before looking at new test scores. Classification-only labelers must not be applied to News's continuous target; any historical row that did this is invalid and must be identified plainly, with a distinctly named regression method if that experiment is run.

## 2. Rebuild splits and preprocessing from original sources

Fix `src/commons/create_train_test.py`: split source records once, without redistributing categories using the test set or duplicating rows. Assign source IDs before MNIST resizing/binarization and other transformations. Group repeated source records or identical processed features before splitting wherever the result claims performance on unseen examples. Rebuild Intrusion: 8,082/10,000 current test rows match training. Check exact feature/full-row overlap and relevant near-duplicates in **both raw and one-hot/model-input files** for every dataset. Save each split's source IDs, row counts, label counts, column names, and hashes.

Reserve real train, dev, and test **before fitting anything**, including imputers, one-hot encoders, scalers, CTGAN/TVAE, PCA/GMM, and synthetic labelers. Fit preprocessing on real train; apply the fitted objects unchanged to dev/test. Handle unseen categories through the encoder instead of moving test rows. Fix the imputer call, use the named target before feature alignment (especially Huy), and correct root `test_option` routing. The test file must be inaccessible to all training and selection code until final evaluation.

Completion check: no source ID crosses splits; no unexpected duplicate/overlap remains under the declared policy; input features and target labels match by name and order; transforms of one test row do not depend on other test rows.

## 3. Fix CTGAN and TVAE generation before the full run

SDV's [CTGAN](https://docs.sdv.dev/sdv/modeling/single-table-synthesizers/ctgansynthesizer.md) and [TVAE](https://docs.sdv.dev/sdv/modeling/single-table-synthesizers/tvaesynthesizer.md) workflows both require metadata, `fit(real_train)`, and `sample(num_rows=...)`. The basic pattern in `src/synthesize_data/synthesizer.py` is recognizable, but its metadata is only auto-detected and its effective settings are not recorded. The [SDV metadata documentation](https://docs.sdv.dev/sdv/integration/metadata.md) says detected types need inspection and correction. For the pinned SDV version, explicitly mark every classification target as categorical, especially numeric `Credit.Class`, MNIST `label`, and Covertype `Cover_Type`; check integer-coded feature categories and IDs. Validate metadata against training data and save it. Credit's previous CTGAN sample had 32.99% fraud labels versus 0.17% in the real test set, making this check essential.

Train **independent full-table CTGAN and TVAE baselines** on real training features plus target. Train **X-only CTGAN and TVAE** on real training features for the relabeling methods. Give each generator an explicit epoch count, batch size, seed, sample count, and device setting supported by the pinned version. Current code comments out `epochs=500`, so the library default is used; current SDV documentation lists a 300-epoch default and deprecates `cuda` in favor of `enable_gpu`. Check actual parameters with `get_parameters()`. Fix `CreateSyntheticData.create_synthetic_data()` so a TVAE run prepares its own data and does not depend on a previous CTGAN run or silently train an unrelated generator. Reuse each fitted X-only generator consistently across its labelers.

Before classifier training, verify generated columns, types, row counts, value ranges, target classes/prevalence, and exact matches to training/dev/test. Record generation quality and disclose a baseline that produces implausible labels; adjust configuration using dev only, then freeze it. Do not use test outcomes to tune synthesis. Never silently replace a failed method with a different one.

## 4. Fix the remaining modeling bugs, then run small end-to-end checks

Repair the confirmed [audit findings](AUDIT.md) in the code paths in the run list: GMM per-row posterior normalization and zero-probability handling; CategoricalNB's use of scaled continuous values; regression splitting/labeling; quiet ensemble metrics; mix sample counts; batching remainders; optimizer reset; invalid options that construct but do not raise `ValueError`; unsupported Credit/Covertype branches; MNIST feature shape and pixel order. Give multiclass models the correct number of outputs and use metrics valid for the task. Remove printing of real rows/features, and load only trusted saved synthesizers/checkpoints.

Select classifier checkpoints and any hyperparameters using the real dev split. Stop evaluating the test set each epoch in Thuy, and replace test-driven checkpoint selection in root/Huy/Sabina. Reload the selected checkpoint for one final test evaluation. Run a small end-to-end check for every dataset/method **before** expensive training: split → fit generator → sample → label → train → dev select → test predict. Use tiny artificial or bounded training data for these checks; assert counts, target identity, output dimensions, finite scores, batch invariance for GMM, and no test access during fitting. Let invalid configurations fail with a direct error; avoid broad `try/except` or fallback data paths.

## 5. Rerun the complete matrix and rebuild results

Run every prespecified valid combination on the newly fixed split. Use the same real dev/test IDs, classifier architecture, training budget, selection rule, sample count, and seed list for CTGAN, TVAE, and the proposed methods within each dataset. Keep full-table CTGAN/TVAE, X-only-generator-plus-labeler, real-only, and mixed-data results in distinct rows. For Credit, report minority-class recall/precision and PR-AUC alongside macro-F1 so an all-negative model cannot look successful. For News, use regression metrics and regression-capable methods.

Save one run record with code version, package versions, split hashes, metadata/parameter files, generator and classifier configuration, seed, selected dev epoch, synthetic label counts, and final row-level test predictions. Build result CSVs/figures from these records. Fix the workbook/notebook aggregation that classified mixed runs as synthetic-only, and never take a maximum over test epochs. Report per-seed results and variation. Recompute overlap-restricted metrics directly from saved predictions when relevant.

Completion check: every new table cell points to a run and prediction file; every planned valid run has completed or is reported as a failure with its specific cause; no historical checkpoint or synthetic CSV is reused as a corrected result; rerunning the result builder reproduces every table and figure. The corrected CTGAN comparison is assessed only after this gate.
